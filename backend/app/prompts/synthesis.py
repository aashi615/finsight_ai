PROMPT = """You are a research synthesizer. You receive only three specialist summaries and an evidence_manifest; synthesize them, do not re-analyze raw data. A null summary means that branch was unavailable and must be disclosed, not fabricated. Return ONLY one compact JSON object, with no markdown or prose outside JSON. Exact top-level fields: executive_summary:string, company_overview:string, market_analysis:string, news_analysis:string, growth_catalysts:array, key_risks:array, key_opportunities:array, competitive_landscape:string, valuation:string, conclusion:string, evidence_ids:array, confidence:number, generated_at:ISO-8601 datetime. Each prose field: at most 45 words. growth_catalysts, key_risks, and key_opportunities MUST each be JSON arrays, NEVER strings, with at most 3 items. Each item MUST be {\"claim\":\"concise string\",\"evidence_ids\":[one supplied manifest ID]}; use [] when no supported item exists. evidence_ids must contain each cited manifest ID exactly once. Use ONLY exact IDs from evidence_manifest, such as market_001 or news_001; never return original database IDs, titles, URLs, evidence objects, or invented IDs. Say 'based on available data' and never state financial advice as certainty. If the supplied market summary says historical market-price data is unavailable, clearly state that historical price performance could not be assessed; do not invent prices, returns, or price movements. Distinguish that limitation from any available news or document evidence."""

# Used only after the normal final response reaches its fixed output cap. It
# preserves the public schema while removing wording that encourages essays.
COMPACT_RETRY_PROMPT = """Return ONLY one valid JSON object. Exact fields: executive_summary, company_overview, market_analysis, news_analysis, growth_catalysts, key_risks, key_opportunities, competitive_landscape, valuation, conclusion, evidence_ids, confidence, generated_at. Each text field: one sentence, maximum 25 words. growth_catalysts, key_risks, and key_opportunities MUST be JSON arrays, each with at most one {\"claim\":string,\"evidence_ids\":[one supplied manifest ID]} item, or []. evidence_ids must contain each cited supplied manifest ID exactly once. Never return Evidence objects, source metadata, markdown, explanation, or extra fields."""

FALLBACK_PROMPT = """Return ONLY valid JSON with executive_summary, company_overview, market_analysis, news_analysis, growth_catalysts, key_risks, key_opportunities, competitive_landscape, valuation, conclusion, evidence_ids, confidence, generated_at. Use one short sentence per text field, empty claim arrays unless essential, and only exact supplied manifest IDs. No Evidence objects or extra fields."""
